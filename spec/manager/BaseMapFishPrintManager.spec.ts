/* eslint-env jest*/
import { BaseMapFishPrintManager } from '../../src/manager/BaseMapFishPrintManager';

describe('BaseMapFishPrintManager', () => {

  it('is defined', () => {
    expect(BaseMapFishPrintManager).not.toBeUndefined();
  });

});
